package xfi.java.util.concurrent.update.index;

import xfi.java.util.concurrent.update.Persistor;

import javax.sql.DataSource;

public class IndexPersistor implements Persistor<Index> {
    public IndexPersistor(DataSource dataSource) {

    }

    @Override
    public void persist(Index thing) {

    }
}
